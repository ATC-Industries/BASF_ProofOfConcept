# basf-xarvio

- Node 22+ required; run `nvm use` before `npm run dev`
- `npm run dev` - local dev server with live reload
- `npm run deploy` - deploy to Cloudflare
- Prereq: `wrangler login` once, first time only
